title: confkit
description: Typed, validated configuration loading for Go
url: https://mimojanra.github.io
baseurl: /confkit
markdown: kramdown
highlighter: rouge

plugins:
  - jekyll-sitemap

exclude:
  - vendor
  - Gemfile
  - Gemfile.lock

defaults:
  - scope:
      path: ""
    values:
      layout: "default"
